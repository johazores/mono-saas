# API base URL (must match app-api port)
NEXT_PUBLIC_API_URL="http://localhost:7001"

# Clerk (optional — only needed if Clerk auth is enabled in admin settings)
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
